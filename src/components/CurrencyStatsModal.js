import React from 'react';
import {Modal} from 'react-native';

export const CurrencyStatsModal = ({
  showModal
}) => {
  return (
    <Modal
      testID="modal"
      animationType="slide"
      visible={showModal}
    >
    
    </Modal>
  );
}
